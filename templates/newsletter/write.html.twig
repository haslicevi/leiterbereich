{% extends 'base.html.twig' %}

{% block pagetitle %}Newsletter{% endblock %}

{% block body %}
<div class="card shadow mb-4">
    <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
        <h6 class="m-0 font-weight-bold text-primary">Newsletter "{{ newsletter.betreff }}" schreiben</h6>
    </div>
    <div class="card-body">
        <a href="{{ path('newsletter') }}" class="btn btn-info btn-icon-split mb-3">
            <span class="icon text-white-50">
                <i class="fas fa-undo-alt"></i>
            </span>
            <span class="text">Zurück</span>
        </a>
        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}

{% block javascripts %}
<script src="{{ asset('summernote/summernote-bs4.min.js') }}"></script>
<script src="{{ asset('summernote/lang/summernote-de-DE.min.js') }}"></script>
<link rel="stylesheet" href="{{ asset('summernote/summernote-bs4.min.css') }}">

<script>
$(document).ready(function() {
    $('#form_nachricht').summernote({
        lang: 'de-DE',
        height: 300,
        toolbar: [
            {# ['style', ['style']], #}
            ['font', ['bold', 'underline', 'clear']],
            {# ['fontname', ['fontname']], #}
            ['color', ['color']],
            ['para', ['ul', 'ol', 'paragraph']],
            ['table', ['table']],
            ['insert', ['link', 'picture', 'video', 'hr']],
            ['misc', ['undo', 'redo']],
            ['view', ['fullscreen', 'codeview', 'help']],
        ],
    });
});
</script>

<!--<script type='text/javascript' src='{{ asset('spellcheck/include.js') }}' ></script>

<script type='text/javascript'>
    $Spelling.DefaultDictionary = 'Deutsch'
    $Spelling.SpellCheckAsYouType('form_nachricht')
</script>-->
{% endblock %}