{% extends 'base.html.twig' %}

{% block pagetitle %}KGH{% endblock %}

{% block body %}
<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">KGH Raumverwaltung</h6>
    </div>
    <div class="card-body">
        <div class="card shadow mb-4 border-bottom-warning">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-warning">Info</h6>
            </div>
            <div class="card-body">
                 <p>Die KGH Raumverwaltung findet im Verwaltungstool der Kirche statt.</p>
                {# <form action="{{ url }}" method="post" target="_blank">
                    <input type="hidden" name="login" value="{{ loginData.user }}">
                    <input type="hidden" name="password" value="{{ loginData.pw }}">
                    <button type="submit" class="btn btn-primary btn-icon-split mb-3">
                        <span class="icon text-white-50">
                            <i class="fas fa-arrow-right"></i>
                        </span>
                        <span class="text">zum Verowa</span>
                    </button>
                </form> #}
            </div>
        </div>
        <h5>Zugangsdaten</h5>
        <table class="table table-striped">
            <tbody>
                <tr>
                    <th scope="row" width="20%">Link</th>
                    <td><a href="https://secure.verowa.ch/admin/index.php?m=refkinini" target="_blank">zum Verowa</a></td>
                </tr>
                <tr>
                    <th scope="row">Benutzername</th>
                    <td>haslicevi</td>
                </tr>
                <tr>
                    <th scope="row">Passwort</th>
                    <td>Pringles00</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
{% endblock %}